name: Trigger Archive Workflow

on:
  schedule:
    - cron: "*/30 * * * *"  # every 30 minutes or whatever
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - uses: peter-evans/schedule-workflow@v1
        with:
          workflow: "archive.yml"
